$gap = 15px

.g__col
  flex-grow 0
  flex-shrink 0
  min-height 1px
  padding-left ($gap / 2)
  padding-right ($gap / 2)

  .g__no-gap &
    padding 0

for num in 1..12
  .g__col--{num}
    flex-basis percentage(num / 12)
    max-width percentage(num / 12 )
